export enum SourceType {
    observable,
    url
}

export enum SortMode {
    asc = 'asc',
    desc = 'desc'
}

export enum FilterOperator {
    eq = 'eq',
    noEq = 'noEq'
}

export enum FilterLogic {
    and = 'and',
    or = 'or'
}